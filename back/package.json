{
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json && mkdir -p dist/db/queries && cp -r src/db/queries/. dist/db/queries/",
    "start": "node dist/server.js",
    "lint": "eslint src"
  },
  "dependencies": {
    "fastify": "^5.10.0",
    "@fastify/helmet": "^13.1.0",
    "@fastify/cors": "^10.0.0",
    "@fastify/rate-limit": "^10.0.0",
    "pg": "^8.22.0",
    "node-cache": "^5.1.2"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.1.1",
    "@types/pg": "^8.20.0",
    "tsx": "^4.19.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.65.0"
  }
}
